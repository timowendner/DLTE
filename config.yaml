train folder: 'MeterTest/trainset'
prediction folder: 'MeterTest/testset'
label file: 'MeterTest/trainset/key-meter_train_gt.txt'
output file: MeterTest/output/outputs.txt

write every n iteration: 10

train split: 0.8
learning rate: 0.001
train epochs: 250
batch size: 16
Data Length: 350
Dropout: 0.25
n order differences: 36
check n times: 10

# the model class: 'CNN', 'RNN'
model class: 'CNN'

# settings for the CNN
kernel size: 5
CNN features: [200, 400, 450, 400, 400, 200]
Output features: [500, 200, 1]

# settings for the RNN
bidirectional: False
hidden size: 500

debug size: 20